import { Placeholder } from '../../../components/Placeholder';

export default function Today() {
  return (
    <Placeholder
      name="Today (2.1)"
      icon="today-outline"
      description="Collect today’s moments → generate a montage."
    />
  );
}
