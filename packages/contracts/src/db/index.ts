// Public DB surface for @twenty4/contracts.
// Re-exports the full Drizzle schema set (the single source of truth / spine).
export * as schema from "./schema/index.ts";
export * from "./schema/index.ts";
